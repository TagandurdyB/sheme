import 'package:flutter/material.dart';

class PageAddExebition extends StatelessWidget {
  const PageAddExebition({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Add Exhebition"),
        centerTitle: true,
      ),
    );
  }
}
